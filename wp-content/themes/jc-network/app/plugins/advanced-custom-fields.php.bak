<?php
/**
 * Project Name:    Discover Jackson NC
 * Project URI:     https://www.discoverjacksonnc.com
 * Description:     Advanced Custom Fields modifications
 * Agency:          Rawle Murdy Associates
 * Agency URI:      https://www.rawlemurdy.com
 * Text Domain:     jctda
 *
 * @package         jctda
 * @since           20190209
 * @author          lpeterson
 */


// Drastically speed up the load times of the post edit page
add_filter( 'acf/settings/remove_wp_meta_box', '__return_true' );

// Hide ACF Plugin Settings from the admin
add_filter( 'acf/settings/show_admin', '__return_true' );

// Google Maps API Key
add_action( 'acf/init', function() {
	acf_update_setting('google_api_key', 'AIzaSyAt8B_YwNZbkQyIhoodSEGwfzrbWwVxn9o');
});

define( 'ACF_PRO_LICENSE', 'b3JkZXJfaWQ9MzQ2NTB8dHlwZT1kZXZlbG9wZXJ8ZGF0ZT0yMDE0LTA3LTEzIDE5OjQ1OjE2' );
add_filter( 'pre_option_acf_pro_license', function( $pre ) {
	if ( !defined('ACF_PRO_LICENSE') || empty(ACF_PRO_LICENSE) )
		return $pre;

	$data = [
		'key' => ACF_PRO_LICENSE,
		'url' => home_url(),
	];

	return base64_encode( serialize( $data ) );
});
