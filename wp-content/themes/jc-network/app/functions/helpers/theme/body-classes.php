<?php
/* ---------------------------------------
*
* Body classes for more precise styling
*
* @package jc-network
*
------------------------------------ */

if ( ! function_exists('jc_network_add_body_class') ) :
/*
* Add the page/post slug to the body class
* Add the post name to the body class
------------------------------------ */
//function jc_network_add_body_class( $classes ) {
	//global $post;

	//if ( isset( $post ) ) {
	//	$classes[]  = $post->post_name . '-' . $post->post_type;
	//	$classes[] .= 'article-' . $post->post_name;
	//}

	//return $classes;
//}
//add_filter( 'body_class', 'jc_network_add_body_class' );
endif;
